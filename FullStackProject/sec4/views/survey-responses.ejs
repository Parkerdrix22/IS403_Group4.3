<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Survey Responses Summary</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">
    <style>
        :root {
            --eq-green: rgb(6, 40, 0);
            --header-height: 4rem;
        }
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            background-color: white;
            color: var(--eq-green);
            letter-spacing: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background-image: url('/login_background.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        header {
            color: black;
            height: var(--header-height);
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            text-align: center;
            background-color: white;
            display: flex;
            align-items: center;
            z-index: 1000;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
        }
        nav {
            display: flex;
            align-items: center;
            width: 100%;
            justify-content: space-between;
            padding: 0 2rem;
        }
        nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            gap: 2rem;
            align-items: center;
        }
        nav ul li {
            display: inline-flex;
        }
        nav ul li a {
            color: var(--eq-green);
            text-decoration: none;
            font-weight: bold;
            font-size: 0.85rem;
        }
        .login-pill {
            background-color: var(--eq-green);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 20px;
            text-decoration: none;
            font-weight: bold;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }
        .login-pill:hover {
            background-color: rgba(6, 40, 0, 0.8);
            color: white;
        }
        main {
            padding: 20px;
            margin-top: calc(var(--header-height) + 1.5rem);
            flex: 1;
            max-width: 1000px;
            margin-left: auto;
            margin-right: auto;
            width: 100%;
        }
        h1 {
            font-family: 'DM Serif Display', serif;
            font-size: clamp(2rem, 6vw, 3.5rem);
            font-weight: 400;
            letter-spacing: 0;
            margin-bottom: 1.5rem;
        }
        .lesson-card {
            background: white;
            border: 2px solid var(--eq-green);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1.25rem;
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        }
        .lesson-card h3 {
            font-family: 'DM Serif Display', serif;
            margin-bottom: 0.5rem;
        }
        .question-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(6,40,0,0.1);
        }
        .question-row:last-child {
            border-bottom: none;
        }
        .avg-pill {
            min-width: 64px;
            text-align: center;
            padding: 0.35rem 0.75rem;
            border-radius: 999px;
            border: 2px solid var(--eq-green);
            font-weight: 700;
            color: var(--eq-green);
            background: rgba(6,40,0,0.06);
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/dashboard">HOME</a></li>
                <li><a href="/upcoming-lesson">UPCOMING LESSONS</a></li>
                <li><a href="/feedback">FEEDBACK</a></li>
                <% if (user && user.isManager) { %>
                <li><a href="/survey-responses">SURVEY RESPONSES</a></li>
                <% } %>
            </ul>
            <a href="/logout" class="login-pill">LOGOUT</a>
        </nav>
    </header>

    <main class="container">
        <h1>Survey Responses</h1>

        <% if (typeof error_message !== 'undefined' && error_message) { %>
            <div class="alert alert-danger"><%= error_message %></div>
        <% } %>

        <% if (!lessons || lessons.length === 0) { %>
            <p>No lessons found.</p>
        <% } else { %>
            <% lessons.forEach(lesson => { %>
                <div class="lesson-card">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h3><%= lesson.lessonname %></h3>
                        <div><strong><%= lesson.date %></strong></div>
                    </div>
                    <% if (lesson.questions && lesson.questions.length > 0) { %>
                        <% lesson.questions.forEach((q, idx) => { %>
                            <div class="question-row">
                                <div><strong><%= idx + 1 %>.</strong> <%= q.text %></div>
                                <div class="avg-pill"><%= q.average %></div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <p>No survey questions defined for this lesson.</p>
                    <% } %>
                </div>
            <% }); %>
        <% } %>
    </main>
</body>
</html>

