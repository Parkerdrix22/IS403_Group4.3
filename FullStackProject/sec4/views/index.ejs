<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elders Quorum</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">
    <style>
        /* Main Content Padding */
        main {
            padding: 20px;
            margin-top: calc(var(--header-height, 4rem) + 1.5rem);
            flex: 1;
            position: relative;
            min-height: 100vh;
        }
        /* Smooth Scrolling */
        html {
            scroll-behavior: smooth;
        }
        /* CSS variables and general body styling */
        :root {
            --header-height: 4rem;
            --footer-height: 5rem;
        }
        /* General Body Styling */
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            background-color: rgb(6, 40, 0);
            background-position: center;
            background-repeat: no-repeat;
            min-height: auto;
            color: white;
            letter-spacing: 0;
            overflow-x: hidden;
            display: flex;
            flex-direction: column;
            width: 100vw;
        }
        /* Header Styling */
        header {
            color: rgb(6, 40, 0);
            height: var(--header-height);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            text-align: center;
            background-color: white;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 2rem;
            box-sizing: border-box;
        }
        /* Footer Styling (Sticky Footer) */
        footer {
            background-color: white;
            color: rgb(6, 40, 0);
            min-height: var(--footer-height);
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            margin-top: auto;
            padding: 1.5rem 2rem;
            border-top: 2px solid rgba(6, 40, 0, 0.2);
        }
        nav {
            display: flex;
            align-items: center;
            width: 100%;
            justify-content: space-between;
        }
        nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            gap: 2rem;
            align-items: center;
        }
        nav ul li {
            display: inline-flex;
        }
        nav ul li a {
            color: rgb(6, 40, 0);
            text-decoration: none;
            font-weight: bold;
            font-size: 0.85rem;
        }
        .login-pill {
            background-color: rgb(6, 40, 0);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 20px;
            text-decoration: none;
            font-weight: bold;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }
        .login-pill:hover {
            background-color: rgba(6, 40, 0, 0.8);
            color: white;
        }
        /* Heading */
        h1 {
            font-family: 'DM Serif Display', serif;
            font-size: clamp(2rem, 8vw, 5rem);
            font-weight: 400;
            letter-spacing: 0;
            position: absolute;
            top: 65vh;
            left: 50%;
            transform: translateX(-50%);
            white-space: nowrap;
        }
        .welcome-message {
            font-family: Arial, sans-serif;
            font-size: clamp(0.875rem, 3.5vw, 1.5rem);
            font-weight: bold;
            letter-spacing: 0;
            position: absolute;
            top: calc(65vh + clamp(2rem, 8vw, 5rem) + 1rem);
            left: 50%;
            transform: translateX(-50%);
            white-space: nowrap;
            color: white;
        }
        /* Left Image Container */
        .left-image-container {
            position: absolute;
            top: 42vh;
            left: 15vw;
            transform: translateY(-50%);
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        /* Left Image Text Styling */
        .left-image-text {
            font-size: clamp(0.875rem, 2.5vw, 1.25rem);
            font-weight: bold;
            color: white;
            letter-spacing: 0;
            white-space: nowrap;
            margin-bottom: 1vh;
        }
        /* Left Image Styling */
        .left-image {
            height: 35vh;
            width: 35vw;
            overflow: hidden;
            border: 3px solid #ffffff;
            border-radius: 10px;
        }
        .left-image img {
            height: 100%;
            width: 100%;
            object-fit: cover;
        }
        /* Right Image Styling */
        .right-image {
            position: absolute;
            top: calc((var(--header-height, 4rem) + 1.5rem) * -1);
            right: -20px;
            height: 100vh;
            max-height: 100vh;
            width: 40%;
            z-index: -1;
            overflow: hidden;
        }
        .right-image img {
            height: 100%;
            width: 100%;
            object-fit: cover;
            display: block;
        }
        
        /* Section Styling */
        .content-section {
            padding: 80px 20px;
            margin: 40px 0;
            position: relative;
            z-index: 1;
        }
        
        .section-title {
            font-family: 'DM Serif Display', serif;
            font-size: clamp(2rem, 6vw, 3.5rem);
            font-weight: 400;
            letter-spacing: 0;
            margin-bottom: 2rem;
            color: white;
        }
        
        .section-content {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            backdrop-filter: blur(10px);
        }
        
        .section-button {
            display: inline-block;
            padding: 15px 40px;
            background-color: white;
            color: rgb(6, 40, 0);
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
            letter-spacing: 0;
            border-radius: 5px;
            margin-top: 20px;
            transition: all 0.3s ease;
            border: 2px solid white;
        }
        
        .section-button:hover {
            background-color: transparent;
            color: white;
            transform: scale(1.05);
        }

        /* Container styling */
        .section-alternate .container {
            max-width: 1400px;
        }

        /* Hero spacer to push content sections down */
        .hero-spacer {
            height: calc(100vh + 2rem);
            position: relative;
            z-index: 0;
        }

        /* Centered sections for upcoming lesson and feedback */
        .section-centered {
            text-align: center;
            padding: 3rem 2rem;
            position: relative;
            z-index: 1;
            margin-top: 0;
            margin-bottom: 4rem;
            width: 100%;
            clear: both;
        }
        
        #upcoming-lesson.section-centered {
            margin-top: 2rem;
            margin-bottom: 0;
            border-bottom: 2px solid rgb(6, 40, 0);
        }
        
        #feedback.section-centered {
            margin-top: 0;
            margin-bottom: 0;
            border-top: 2px solid rgb(6, 40, 0);
            border-bottom: 2px solid rgb(6, 40, 0);
        }

        #ward-info.section-centered {
            margin-top: 0;
            margin-bottom: 2rem;
            border-top: 2px solid rgb(6, 40, 0);
        }

        .section-centered .container {
            max-width: 1200px;
            width: 100%;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .section-centered h2 {
            font-family: 'DM Serif Display', serif;
            font-weight: 700;
            margin-bottom: 1rem;
            font-size: 1.75rem;
            color: rgb(6, 40, 0);
        }

        .section-centered p {
            max-width: 600px;
            margin: 0 auto 1.5rem;
            font-size: 1rem;
            line-height: 1.6;
            color: rgb(6, 40, 0);
        }

        /* Button styling */
        .section-centered .btn {
            padding: 0.75rem 2rem;
            font-size: 1rem;
            font-weight: 500;
            letter-spacing: 0;
            border: 2px solid rgb(6, 40, 0);
            color: rgb(6, 40, 0);
            background-color: transparent;
            transition: all 0.3s ease;
        }

        .section-centered .btn:hover {
            background-color: rgb(6, 40, 0);
            color: white;
            transform: scale(1.05);
        }

        /* Background colors for sections */
        #upcoming-lesson.section-centered {
            background-image: url('/login_background.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        #feedback.section-centered {
            background-image: url('/login_background.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        #ward-info.section-centered {
            background-image: url('/login_background.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        /* Centered feedback section */
        .feedback-container {
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
        }

        .ward-info-container {
            max-width: 1200px;
            margin: 0 auto;
            text-align: center;
        }

        .ward-info-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
            margin-top: 2rem;
        }

        .ward-info-item {
            text-align: center;
        }

        .ward-info-item h4 {
            font-family: 'DM Serif Display', serif;
            font-size: 1.3rem;
            margin-bottom: 0.5rem;
            color: rgb(6, 40, 0);
            letter-spacing: 0;
        }

        .ward-info-item p {
            color: rgb(6, 40, 0);
            font-size: 1rem;
            line-height: 1.6;
            letter-spacing: 0;
            margin: 0.25rem 0;
        }

        @media (max-width: 768px) {
            .ward-info-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
        }

        /* Two-column layout for upcoming lesson section */
        .upcoming-lesson-container {
            display: flex;
            gap: 4rem;
            align-items: flex-start;
            max-width: 1200px;
            margin: 0 auto;
            position: relative;
        }

        .upcoming-lesson-container::before {
            content: '';
            position: absolute;
            left: 50%;
            top: 0;
            bottom: 0;
            width: 1px;
            background-color: rgb(6, 40, 0);
            transform: translateX(-50%);
        }

        .upcoming-lesson-left {
            flex: 1;
            text-align: left;
        }

        .upcoming-lesson-right {
            flex: 1;
        }

        .lesson-card-preview {
            background-color: transparent;
            border: 2px solid rgb(6, 40, 0);
            border-radius: 10px;
            padding: 1.5rem;
            text-align: left;
            text-decoration: none;
            color: rgb(6, 40, 0);
            display: block;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .lesson-card-preview:hover {
            background-color: rgba(6, 40, 0, 0.1);
            transform: translateY(-3px);
        }

        .lesson-card-preview h3 {
            font-family: 'DM Serif Display', serif;
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: rgb(6, 40, 0);
            letter-spacing: 0;
        }

        .lesson-card-preview .lesson-date {
            font-size: 1rem;
            margin-bottom: 1rem;
            color: rgb(6, 40, 0);
            font-weight: 600;
            letter-spacing: 0;
        }

        .lesson-card-preview .lesson-description {
            font-size: 0.95rem;
            line-height: 1.6;
            color: rgb(6, 40, 0);
            letter-spacing: 0;
        }

        @media (max-width: 768px) {
            .upcoming-lesson-container {
                flex-direction: column;
                gap: 2rem;
            }
            .upcoming-lesson-container::before {
                display: none;
            }
        }

        /* Responsive adjustments */
        @media (max-width: 992px) {
            :root {
                --header-height: 3.5rem;
                --footer-height: 3.5rem;
            }
            body {
                min-height: auto;
            }
            header {
                position: fixed;
                width: 100%;
                left: 0;
                right: 0;
                z-index: 1000;
            }
            main {
                margin-top: calc(var(--header-height) + 1rem);
                padding: 16px;
            }
            h1 {
                position: static;
                transform: none;
                font-size: clamp(1.75rem, 8vw, 3rem);
                letter-spacing: 0;
                margin: 10vh 0 1rem 0;
                white-space: normal;
            }
            .welcome-message {
                position: static;
                transform: none;
                font-size: clamp(0.75rem, 3vw, 1.25rem);
                letter-spacing: 0;
                margin: 0 0 2rem 0;
                white-space: normal;
            }
            .left-image-container {
                position: static;
                top: auto;
                left: auto;
                transform: none;
                margin: 0 auto 2rem auto;
                width: 100%;
                max-width: 800px;
                padding: 0; /* remove side padding so content is truly centered */
            }
            .left-image-text {
                font-size: clamp(1rem, 4vw, 1.125rem);
                letter-spacing: 0;
            }
            .left-image {
                width: min(92vw, 600px);
                height: auto;
                margin: 0 auto; /* center the image container */
                border-radius: 10px;
            }
            .left-image img {
                display: block; /* remove inline-gap */
                width: 100%;
                height: auto;
                margin: 0 auto; /* ensure centered image */
            }
            .right-image {
                display: none;
            }
        }

        @media (max-width: 576px) {
            :root {
                --header-height: 3.25rem;
                --footer-height: 3.25rem;
            }
            nav ul {
                padding-left: 1rem;
                gap: 1rem;
            }
            h1 {
                font-size: clamp(1.5rem, 10vw, 2.25rem);
                letter-spacing: 0;
                margin-top: 12vh;
            }
            .welcome-message {
                font-size: clamp(0.7rem, 4vw, 1rem);
                letter-spacing: 0;
                margin-top: 0.5rem;
            }
            .left-image-text {
                font-size: clamp(0.9rem, 5vw, 1rem);
            letter-spacing: 0;
            }
            .left-image {
                width: 90vw; /* slightly narrower on very small screens */
            }
            .content-section {
                padding: 60px 16px;
                margin: 30px 0;
            }
            .section-content {
                padding: 30px 20px;
            }
            .section-button {
                padding: 12px 30px;
                font-size: 1rem;
            }
            .section-centered {
                padding: 3rem 1rem;
            }
            .hero-spacer {
                height: auto;
                min-height: calc(100vh + 2rem);
            }
            .section-centered {
                padding: 2rem 1rem;
                margin-bottom: 2rem;
            }
            .section-centered h2 {
                font-size: 1.5rem;
            }
            .section-centered p {
                font-size: 0.9rem;
                margin-bottom: 1rem;
            }
        }
    </style>
    <script>
        // Prevent editing persistence for contenteditable placeholders on reload (optional safety)
        document.addEventListener('DOMContentLoaded', function() {
            // no-op placeholder
        });
    </script>
</head>
<body>
    <!-- Header -->
    <header>
        <nav>
            <ul>
                <li><a href="/dashboard">HOME</a></li>
                <li><a href="/upcoming-lesson">UPCOMING LESSONS</a></li>
                <li><a href="/feedback">FEEDBACK</a></li>
            </ul>
            <a href="/logout" class="login-pill">LOGOUT</a>
        </nav>
    </header>
    <!-- Main Content -->
    <main>
        <h1 style="letter-spacing: 0.25rem;">Elders Quorum</h1>
        <% if (user && user.memberfirstname && user.memberlastname) { %>
            <div class="welcome-message">Welcome, <%= user.memberfirstname %> <%= user.memberlastname %></div>
        <% } %>
        <!-- Left Image with Words Above it -->
        <div class="left-image-container">
            <div class="left-image-text">
                <p style="letter-spacing: 0.25rem;">HILLCREST 8th WARD</p>
            </div>
            <div class="left-image">
                <img src="/left-image.jpg" alt="Left Side Image">
            </div>
        </div>
        <!-- Image on the right side -->
        <div class="right-image">
            <img src="/right-image.jpg" alt="Right Side Image">
        </div>
        
        <!-- Spacer to push sections below hero -->
        <div class="hero-spacer"></div>
        
        <!-- Upcoming Lesson Section -->
        <section id="upcoming-lesson" class="section-centered">
            <div class="container">
                <div class="upcoming-lesson-container">
                    <!-- Left Side: Info and Button -->
                    <div class="upcoming-lesson-left">
                        <h3 style="font-family: 'DM Serif Display', serif; font-size: 2rem; margin-bottom: 1rem; color: rgb(6, 40, 0);">
                            Upcoming Lessons
                        </h3>
                        <p style="margin-bottom: 1.5rem; color: rgb(6, 40, 0); line-height: 1.6;">
                            Stay informed about our upcoming lessons and prepare to participate in meaningful discussions. View all lessons and access reading materials and discussion questions.
                        </p>
                        <a href="/upcoming-lesson" class="btn btn-outline-light btn-lg">
                            View All Upcoming Lessons
                        </a>
                    </div>
                    
                    <!-- Right Side: Upcoming Lesson Card -->
                    <div class="upcoming-lesson-right">
                        <% if (typeof upcomingLesson !== 'undefined' && upcomingLesson) { %>
                            <a href="/upcoming-lesson/<%= upcomingLesson.lessonid %>" class="lesson-card-preview">
                                <h3><%= upcomingLesson.lessonname %></h3>
                                <div class="lesson-date">
                                    <%= upcomingLesson.date %>
                                </div>
                                <% if (upcomingLesson.description) { %>
                                    <div class="lesson-description">
                                        <%= upcomingLesson.description %>
                                    </div>
                                <% } else { %>
                                    <div class="lesson-description">
                                        Click to view lesson details, reading materials, and discussion questions.
                                    </div>
                                <% } %>
                            </a>
                        <% } else { %>
                            <div class="lesson-card-preview" style="opacity: 0.7;">
                                <h3>No Upcoming Lesson</h3>
                                <div class="lesson-description">
                                    Check back soon for upcoming lessons.
                                </div>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Feedback Section -->
        <section id="feedback" class="section-centered">
            <div class="container">
                <div class="feedback-container">
                    <h3 style="font-family: 'DM Serif Display', serif; font-size: 2rem; margin-bottom: 1rem; color: rgb(6, 40, 0);">
                        Feedback
                    </h3>
                    <p style="margin-bottom: 1.5rem; color: rgb(6, 40, 0); line-height: 1.6;">
                        Share your thoughts, suggestions, and feedback to help us improve our quorum meetings. Your input helps us create better experiences for everyone.
                    </p>
                    <a href="/feedback" class="btn btn-outline-light btn-lg">
                        Submit Feedback
                    </a>
                </div>
            </div>
        </section>

        <!-- Ward Info Section -->
        <section id="ward-info" class="section-centered">
            <div class="container">
                <div class="ward-info-container">
                    <h3 style="font-family: 'DM Serif Display', serif; font-size: 2rem; margin-bottom: 2rem; color: rgb(6, 40, 0); letter-spacing: 0;">
                        Ward Info
                    </h3>
                    <div class="ward-info-grid">
                        <div class="ward-info-item">
                            <h4>Location</h4>
                            <p>1035 S 800 E, Orem, UT 84097</p>
                        </div>
                        <div class="ward-info-item">
                            <h4>Meeting Times</h4>
                            <p>Sacrament Meeting: 9:00 AM</p>
                            <p>Elders Quorum Meeting: 10:00 AM</p>
                        </div>
                        <div class="ward-info-item">
                            <h4>Bishop</h4>
                            <p>Bishop Karren</p>
                            <p>Phone: (801) 555-0123</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>
    <!-- Footer -->
    <footer>
        <p>&copy; 2025 HILLCREST 8TH WARD ELDERS QUORUM</p>
    </footer>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>