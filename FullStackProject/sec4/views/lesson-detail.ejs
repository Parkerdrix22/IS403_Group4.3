<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lesson Details - Elders Quorum</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&display=swap" rel="stylesheet">
    <style>
        :root {
            --eq-green: rgb(6, 40, 0);
            --eq-green-dark: rgb(3, 20, 0);
            --eq-text: #ffffff;
            --eq-white: #ffffff;
        }

        body {
            background-image: url('/login_background.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            color: var(--eq-text);
            min-height: 100vh;
            padding: 0;
            margin: 0;
        }

        /* Nav Bar Styling - Full Width */
        header {
            color: black;
            height: 4rem;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            text-align: center;
            background-color: white;
            display: flex;
            align-items: center;
            z-index: 1000;
        }

        nav {
            display: flex;
            align-items: center;
            width: 100%;
            justify-content: space-between;
            padding: 0 2rem;
        }

        nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        nav ul li {
            display: inline-flex;
        }

        nav ul li a {
            color: rgb(6, 40, 0);
            text-decoration: none;
            font-weight: bold;
            font-size: 0.85rem;
        }

        .login-pill {
            background-color: rgb(6, 40, 0);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 20px;
            text-decoration: none;
            font-weight: bold;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .login-pill:hover {
            background-color: rgba(6, 40, 0, 0.8);
            color: white;
        }

        .lesson-detail-container {
            max-width: 900px;
            margin: 0 auto;
            background-color: var(--eq-green);
            border-radius: 1rem;
            padding: 3rem;
            margin-top: 6rem;
        }

        h1 {
            font-family: 'DM Serif Display', serif;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            text-align: center;
        }

        .lesson-info {
            background-color: rgba(255, 255, 255, 0.05);
            border-radius: 0.5rem;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .lesson-info-item {
            margin-bottom: 1rem;
            font-size: 1.1rem;
        }

        .lesson-info-item strong {
            color: var(--eq-white);
            margin-right: 0.5rem;
        }

        .lesson-description {
            background-color: rgba(255, 255, 255, 0.05);
            border-radius: 0.5rem;
            padding: 2rem;
            margin-bottom: 2rem;
            line-height: 1.8;
            font-size: 1.1rem;
        }

        .back-button {
            display: inline-block;
            padding: 0.75rem 2rem;
            background-color: var(--eq-white);
            color: var(--eq-green);
            text-decoration: none;
            font-weight: 600;
            border-radius: 0.5rem;
            transition: all 0.3s ease;
        }

        .back-button:hover {
            background-color: rgba(255, 255, 255, 0.9);
            transform: scale(1.05);
        }

        @media (max-width: 768px) {
            .lesson-detail-container {
                padding: 1.5rem;
                margin-top: 5rem;
            }

            h1 {
                font-size: 2rem;
            }

            nav {
                padding: 0 1rem;
            }

            nav ul {
                gap: 1rem;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <nav>
            <ul>
                <li><a href="/dashboard">HOME</a></li>
                <li><a href="/upcoming-lesson">UPCOMING LESSONS</a></li>
                <li><a href="/feedback">FEEDBACK</a></li>
                <% if (typeof user !== 'undefined' && user && user.isManager) { %>
                <li><a href="/survey-responses">SURVEY RESPONSES</a></li>
                <% } %>
            </ul>
            <a href="/logout" class="login-pill">LOGOUT</a>
        </nav>
    </header>
    <div class="lesson-detail-container">
        <% if (lesson) { %>
            <h1><%= lesson.lessonname || 'Untitled Lesson' %></h1>
            
            <div class="lesson-info">
                <% if (lesson.date) { %>
                    <div class="lesson-info-item">
                        <strong>Date:</strong> <%= lesson.date %>
                    </div>
                <% } %>
                <% if (lesson.teacher) { %>
                    <div class="lesson-info-item">
                        <strong>Teacher:</strong> <%= lesson.teacher %>
                    </div>
                <% } %>
                <% if (lesson.topics) { %>
                    <div class="lesson-info-item">
                        <strong>Topic:</strong> <%= lesson.topics %>
                    </div>
                <% } %>
                <% if (lesson.location) { %>
                    <div class="lesson-info-item">
                        <strong>Location:</strong> <%= lesson.location %>
                    </div>
                <% } %>
            </div>

            <div class="lesson-description">
                <h2 style="font-family: 'DM Serif Display', serif; margin-bottom: 1rem; font-size: 1.8rem;">Lesson Overview</h2>
                <p><%= lesson.description || 'No description available for this lesson.' %></p>
                
                <% if (lesson.reading) { %>
                    <div style="margin-top: 2rem;">
                        <h3 style="font-family: 'DM Serif Display', serif; margin-bottom: 0.5rem;">Reading Materials</h3>
                        <p><%= lesson.reading %></p>
                    </div>
                <% } %>

                <% if (lesson.discussion) { %>
                    <div style="margin-top: 2rem;">
                        <h3 style="font-family: 'DM Serif Display', serif; margin-bottom: 0.5rem;">Discussion Questions</h3>
                        <p><%= lesson.discussion %></p>
                    </div>
                <% } %>
            </div>
        <% } else { %>
            <h1>Lesson Not Found</h1>
            <p>The lesson you're looking for doesn't exist.</p>
        <% } %>

        <a href="/upcoming-lesson" class="back-button">Back to Upcoming Lessons</a>
    </div>
</body>
</html>

