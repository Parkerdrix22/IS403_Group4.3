<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback - Elders Quorum</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --header-height: 4rem;
            --footer-height: 5rem;
        }
        
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            background-color: white;
            color: rgb(6, 40, 0);
            letter-spacing: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background-image: url('/login_background.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        
        header {
            color: black;
            height: var(--header-height);
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            text-align: center;
            background-color: white;
            display: flex;
            align-items: center;
            z-index: 1000;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
        }
        
        nav {
            display: flex;
            align-items: center;
            width: 100%;
            justify-content: space-between;
            padding: 0 2rem;
        }
        
        nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            gap: 2rem;
            align-items: center;
        }
        
        nav ul li {
            display: inline-flex;
        }
        
        nav ul li a {
            color: rgb(6, 40, 0);
            text-decoration: none;
            font-weight: bold;
            font-size: 0.85rem;
        }

        .login-pill {
            background-color: rgb(6, 40, 0);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 20px;
            text-decoration: none;
            font-weight: bold;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .login-pill:hover {
            background-color: rgba(6, 40, 0, 0.8);
            color: white;
        }

        /* Teacher actions */
        .lesson-card {
            position: relative;
        }

        .lesson-actions {
            position: absolute;
            top: 12px;
            right: 12px;
            display: flex;
            gap: 8px;
            z-index: 2;
        }

        .action-btn {
            border: 2px solid rgb(6, 40, 0);
            background: white;
            color: rgb(6, 40, 0);
            border-radius: 6px;
            width: 32px;
            height: 32px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .action-btn:hover {
            background: rgb(6, 40, 0);
            color: white;
        }

        /* Modal tweaks to match lesson editor */
        .modal-content {
            border: 2px solid rgb(6, 40, 0);
            border-radius: 12px;
        }
        .modal-title {
            font-family: 'DM Serif Display', serif;
            color: rgb(6, 40, 0);
        }
        .modal-body label {
            font-weight: 700;
        }
        .modal-body input,
        .modal-body textarea {
            border: 2px solid rgb(6, 40, 0);
        }
        
        main {
            padding: 20px;
            margin-top: calc(var(--header-height) + 2rem);
            flex: 1;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
            width: 100%;
        }
        
        h1 {
            font-family: 'DM Serif Display', serif;
            font-size: clamp(2rem, 6vw, 3.5rem);
            font-weight: 400;
            letter-spacing: 0;
            margin-bottom: 2rem;
        }
        
        .feedback-form {
            background-color: white;
            border: 2px solid rgb(6, 40, 0);
            border-radius: 10px;
            padding: 40px;
            margin: 20px 0;
            text-align: left;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            font-size: 1.1rem;
        }
        
        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 2px solid rgb(6, 40, 0);
            border-radius: 5px;
            background-color: white;
            color: rgb(6, 40, 0);
            font-size: 1rem;
            font-family: Arial, sans-serif;
            box-sizing: border-box;
        }
        
        .form-group input::placeholder,
        .form-group textarea::placeholder {
            color: rgba(6, 40, 0, 0.5);
        }
        
        .form-group textarea {
            min-height: 150px;
            resize: vertical;
        }
        
        .submit-button {
            display: inline-block;
            padding: 15px 40px;
            background-color: rgb(6, 40, 0);
            color: white;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
            letter-spacing: 0;
            border-radius: 5px;
            border: 2px solid rgb(6, 40, 0);
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 10px;
        }
        
        .submit-button:hover {
            background-color: transparent;
            color: rgb(6, 40, 0);
            transform: scale(1.02);
        }
        
        .back-button {
            display: inline-block;
            padding: 15px 40px;
            background-color: transparent;
            color: rgb(6, 40, 0);
            text-decoration: none;
            font-weight: bold;
            font-size: 1rem;
            letter-spacing: 0;
            border-radius: 5px;
            margin-top: 20px;
            transition: all 0.3s ease;
            border: 2px solid rgb(6, 40, 0);
        }
        
        .back-button:hover {
            background-color: rgb(6, 40, 0);
            color: white;
            transform: scale(1.05);
        }
        
        .success-message {
            background-color: rgba(6, 40, 0, 0.1);
            border: 2px solid rgb(6, 40, 0);
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 20px;
            color: rgb(6, 40, 0);
        }
        
        
        @media (max-width: 768px) {
            nav {
                padding: 0 1rem;
            }
            
            nav ul {
                gap: 1rem;
            }
            
            .feedback-form {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/dashboard">HOME</a></li>
                <li><a href="/upcoming-lesson">UPCOMING LESSONS</a></li>
                <li><a href="/feedback">FEEDBACK</a></li>
            </ul>
            <a href="/logout" class="login-pill">LOGOUT</a>
        </nav>
    </header>
    
    <main>
        <h1>Lesson Surveys</h1>
        
        <% if (typeof success !== 'undefined' && success) { %>
        <div class="success-message">
            <p><strong>Success!</strong> <%= typeof message !== 'undefined' ? message : 'Thank you for your feedback!' %></p>
        </div>
        <% } %>
        
        <% if (typeof error !== 'undefined' && error) { %>
        <div class="success-message" style="background-color: rgba(244, 67, 54, 0.1); border-color: rgba(244, 67, 54, 0.8); color: rgb(6, 40, 0);">
            <p><strong>Error:</strong> <%= typeof message !== 'undefined' ? message : 'An error occurred. Please try again.' %></p>
        </div>
        <% } %>

        <!-- Survey Section -->
        <div style="margin-bottom: 3rem; text-align: left;">
            <h2 style="font-family: 'DM Serif Display', serif; font-size: 1.75rem; margin-bottom: 1.5rem; color: rgb(6, 40, 0); text-align: center;">Choose a Lesson to Take the Survey</h2>
            <% if (typeof lessons !== 'undefined' && lessons && lessons.length > 0) { %>
                <div style="display: grid; gap: 1rem;">
                    <% lessons.forEach(lesson => { %>
        <div class="lesson-card"
                             data-lesson-id="<%= lesson.lessonid %>"
                             data-lesson-title="<%- encodeURIComponent(lesson.lessonname || '') %>"
                             style="background-color: white; border: 2px solid rgb(6, 40, 0); border-radius: 10px; padding: 1.5rem;">
                            <% if (typeof isTeacher !== 'undefined' && isTeacher) { %>
                                <div class="lesson-actions">
                                    <button class="action-btn" title="Edit lesson"
                                        onclick="event.preventDefault(); openEditFromCard(this)">
                                        ✎
                                    </button>
                                    <button class="action-btn" title="Delete lesson"
                                        onclick="event.preventDefault(); confirmDeleteFromCard(this)">
                                        ×
                                    </button>
                                </div>
                            <% } %>
                            <h3 style="font-family: 'DM Serif Display', serif; font-size: 1.3rem; margin-bottom: 0.5rem; color: rgb(6, 40, 0);"><%= lesson.lessonname %></h3>
                            <p style="margin: 0.25rem 0; color: rgb(6, 40, 0);"><strong>Date:</strong> <%= lesson.date %></p>
                            <% if (lesson.teacher && lesson.teacher !== 'N/A') { %>
                                <p style="margin: 0.25rem 0; color: rgb(6, 40, 0);"><strong>Teacher:</strong> <%= lesson.teacher %></p>
                            <% } %>
                            <a href="/feedback/survey/<%= lesson.lessonid %>" style="display: inline-block; margin-top: 1rem; background-color: rgb(6, 40, 0); color: white; padding: 0.75rem 2rem; border-radius: 5px; text-decoration: none; font-weight: bold;">Take Survey</a>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p style="text-align: center; color: rgb(6, 40, 0);">No lessons available for survey.</p>
            <% } %>
        </div>

    </main>

    <% if (typeof isTeacher !== 'undefined' && isTeacher) { %>
    <!-- Edit Survey Questions Modal (Bootstrap style) -->
    <div class="modal fade" id="lessonModal" tabindex="-1" aria-labelledby="lessonModalTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="lessonModalTitle">Edit Survey Questions</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form id="lessonForm" onsubmit="handleLessonSubmit(event)">
                    <div class="modal-body">
                        <% for (let i = 1; i <= 5; i++) { %>
                        <div class="mb-3">
                            <label for="question_field_<%= i %>" class="form-label">Question <%= i %></label>
                            <input type="text" class="form-control question-input" id="question_field_<%= i %>" name="question_field_<%= i %>">
                        </div>
                        <% } %>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <% } %>

    <% if (typeof isTeacher !== 'undefined' && isTeacher) { %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const lessonModalEl = document.getElementById('lessonModal');
        const lessonForm = document.getElementById('lessonForm');
        let lessonModalInstance = null;

        function getLessonModal() {
            if (!lessonModalEl) return null;
            if (lessonModalInstance) return lessonModalInstance;
            if (window.bootstrap && bootstrap.Modal) {
                lessonModalInstance = new bootstrap.Modal(lessonModalEl);
                return lessonModalInstance;
            }
            console.error('Bootstrap modal not available');
            return null;
        }

        function decodeField(val) {
            try { return decodeURIComponent(val || ''); } catch (e) { return val || ''; }
        }

        function openEditFromCard(btn) {
            const card = btn.closest('.lesson-card');
            if (!card) return;
            const id = card.dataset.lessonId;
            lessonForm.setAttribute('data-lesson-id', id);
            lessonForm.setAttribute('data-mode', 'edit');
            document.getElementById('lessonModalTitle').textContent = 'Edit Survey Questions';

            // Clear fields while we fetch
            lessonForm.querySelectorAll('input, textarea').forEach(el => el.value = '');

            fetch('/feedback/survey/' + id + '/questions-json')
                .then(res => res.json())
                .then(data => {
                    const qs = (data && data.questions) ? data.questions : [];
                    // Fill up to 5 fields (you can add more inputs if desired)
                    for (let i = 0; i < 5; i++) {
                        const input = document.getElementById('question_field_' + (i + 1));
                        if (input) {
                            input.value = qs[i] || '';
                        }
                    }
                })
                .catch(err => {
                    console.error('Error loading survey questions', err);
                    alert('Error loading survey questions.');
                })
                .finally(() => {
                    const modal = getLessonModal();
                    if (modal) modal.show();
                });
        }

        function closeLessonModal() {
            const modal = getLessonModal();
            if (modal) {
                modal.hide();
            }
            lessonForm.reset();
            lessonForm.removeAttribute('data-lesson-id');
            lessonForm.removeAttribute('data-mode');
        }

        function confirmDeleteFromCard(btn) {
            const card = btn.closest('.lesson-card');
            if (!card) return;
            const lessonId = card.dataset.lessonId;
            const lessonName = decodeField(card.dataset.lessonTitle) || 'this lesson';
            if (confirm('Delete "' + lessonName + '"?\\n\\nThis cannot be undone.')) {
                fetch('/upcoming-lesson/' + lessonId, {
                    method: 'DELETE',
                    headers: { 'Content-Type': 'application/json' }
                })
                .then(res => res.json())
                .then(data => {
                    if (data.success) {
                        alert('Lesson deleted. Refreshing...');
                        window.location.reload();
                    } else {
                        alert('Error: ' + (data.error || 'Failed to delete lesson'));
                    }
                })
                .catch(err => {
                    console.error(err);
                    alert('Error deleting lesson. Please try again.');
                });
            }
        }

        function handleLessonSubmit(event) {
            event.preventDefault();
            const mode = lessonForm.getAttribute('data-mode');
            const lessonId = lessonForm.getAttribute('data-lesson-id');

            // collect question fields
            const payload = new URLSearchParams();
            lessonForm.querySelectorAll('.question-input').forEach((el, idx) => {
                if (el.value && el.value.trim().length > 0) {
                    payload.append('question_' + (idx + 1), el.value.trim());
                }
            });

            fetch('/feedback/survey/' + lessonId + '/questions', {
                method: 'POST',
                headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                body: payload
            })
            .then(res => res.json())
            .then(data => {
                if (!data || data.error) {
                    alert('Error: ' + (data?.error || 'Unable to save questions'));
                } else {
                    alert('Survey questions updated. Refreshing...');
                    window.location.reload();
                }
            })
            .catch(err => {
                console.error(err);
                alert('Error saving questions. Please try again.');
            })
            .finally(() => closeLessonModal());
        }
    </script>
    <% } %>
</body>
</html>

